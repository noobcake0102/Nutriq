app_identifier("com.nutriqai.app")       # The bundle identifier of your app

# Apple Developer Team ID — set ASC_TEAM_ID in fastlane/.env (Membership page in
# the Apple Developer portal). Used for code signing during the archive.
team_id(ENV["ASC_TEAM_ID"])

# App Store Connect authentication is handled by the App Store Connect API key
# (see fastlane/.env). No apple_id / itc_team_id needed with API-key auth.
